<?php
/**
 * Stands in for a bundle manifest generated by @wordpress/scripts.
 *
 * Committed rather than written at runtime so the test measures the same bytes
 * on every machine, and so a reader can see the exact shape the loader parses.
 *
 * @package MHMUiCore
 */

declare(strict_types=1);

return array(
	'dependencies' => array( 'wp-element', 'wp-api-fetch', 'wp-i18n' ),
	'version'      => 'a1b2c3d4e5f6',
);
