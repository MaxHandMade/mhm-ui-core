<?xml version="1.0"?>
<ruleset name="MHM UI Core">
	<description>WordPress Coding Standards for mhm/ui-core.</description>

	<!--
	register.php and bootstrap.php are listed explicitly, not just <file>src</file>:
	they hold every global this package declares (both functions, the global
	variable, both constants), so leaving them out made the PrefixAllGlobals rule
	below blind to the only code it exists to check. src/ alone is namespaced,
	and namespaced code is exempt from that sniff.
	-->
	<file>src</file>
	<file>register.php</file>
	<file>bootstrap.php</file>

	<exclude-pattern>*/vendor/*</exclude-pattern>

	<arg name="extensions" value="php"/>
	<arg value="ps"/>

	<!--
	WordPress-Extra rather than WordPress-Core, and the reason is not taste.

	This package ships INSIDE consuming plugins, under their vendor/mhm/, and
	their gates lint what they find there. mhm-rentiva's security shape gate
	scans vendor/mhm/ with a wider ruleset than this file used to run, so a
	shape this package considered clean turned that consumer's CI red: an
	un-escaped exception message (WordPress.Security.EscapeOutput), measured
	2026-08-27 on the v0.4.0 loader.

	A package cannot lint more loosely than the trees it ships into. The finding
	does not disappear; it moves downstream, where it costs a release cycle
	instead of a commit. Measured before widening: WordPress-Extra adds exactly
	one real finding to this package, plus the two file-naming sniffs already
	excluded below.
	-->
	<rule ref="WordPress-Extra">
		<!--
		This package is composer/PSR-4 autoloaded (see autoload.psr-4 in
		composer.json), not classically included by WordPress. PSR-4 requires
		the file name to match the class name exactly (VersionSelector.php for
		class VersionSelector); WordPress's class-{name}.php / hyphenated-
		lowercase file-naming convention would break autoloading. Excluded
		deliberately, not an oversight.
		-->
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
		<exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
	</rule>
	<rule ref="WordPress-Docs"/>

	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="mhmuicore"/>
				<element value="MHMUiCore"/>
			</property>
		</properties>
	</rule>

	<config name="testVersion" value="8.1-"/>
</ruleset>
